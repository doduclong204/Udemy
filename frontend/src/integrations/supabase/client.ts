// ============================================
// Supabase Client Placeholder
// ============================================
// 
// File này là placeholder cho việc tích hợp Supabase sau này
// Hiện tại project đang sử dụng mock data và sẽ kết nối Spring Boot
// 
// TODO: Nếu cần dùng Supabase, uncomment và cấu hình như sau:
//
// import { createClient } from '@supabase/supabase-js';
//
// const supabaseUrl = import.meta.env.VITE_SUPABASE_URL;
// const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY;
//
// export const supabase = createClient(supabaseUrl, supabaseAnonKey);
//
// ============================================

// Placeholder export để tránh lỗi import
export const supabase = null;

// Type placeholder
export type SupabaseClient = typeof supabase;
